@{
    Layout = null; // Không sử dụng layout
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ManagerUser</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/admin.css" rel="stylesheet" />
    <link href="~/css/manageruser.css" rel="stylesheet" />
     
    
</head>

<body>
    @Html.Partial("Menuleft")
  <div class="content">
        <h1 class="text-center">Quản lý người dùng</h1>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Phone Number</th>
                    <th>Role</th>
                    <th>Created At</th>
                    <th>Actions</th>
                </tr>
            </thead>
             <tbody>
                @if (ViewBag.Users != null)
                {
                    foreach (var user in ViewBag.Users)
                    {
                        <tr>
                            <td>@user.Id</td>
                            <td>@user.User1</td>
                            <td>@user.Fullname</td>
                            <td>@user.Email</td>
                            <td>@user.PhoneNumber</td>
                            <td>@(user.IsAdmin ? "Admin" : "User")</td>
                            <td>@user.createdAt.ToString("yyyy-MM-dd HH:mm:ss")</td>
                            <td>
                                <a href="/Admin/EditUser/@user.Id" class="btn btn-warning btn-sm">Edit</a>
                                <a href="/Admin/DeleteUser/@user.Id" class="btn btn-danger btn-sm">Delete</a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="text-center">No users found.</td>
                    </tr>
                }
            </tbody>
        </table>

        <!-- Phân trang -->
        <div class="pagination">
            @if (ViewBag.Num != null && ViewBag.Num > 1)
            {
                <ul>
                    @for (int i = 1; i <= ViewBag.Num; i++)
                    {
                        <li class="@(ViewBag.Page == i ? "active" : "")">
                            <a asp-controller="Admin" asp-action="ManagerUser" asp-route-xpage="@i">@i</a>
                        </li>
                    }
                </ul>
            }
        </div>
    </div> -->
</body>
</html>
